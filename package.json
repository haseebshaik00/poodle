{
  "name": "poodle",
  "version": "1.0.0",
  "description": "login into root with password\r ``` sql\r CREATE DATABASE poodleDB;\r CREATE USER poodleUser IDENTIFIED BY 'poodle123';\r USE poodleDB;\r GRANT ALL PRIVILEGES ON poodleDB.* TO poodleUser;\r FLUSH PRIVILEGES;\r ```",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.3",
    "hbs": "^4.2.0",
    "mysql2": "^2.3.3",
    "sequelize": "^6.19.0"
  }
}
